[project]
name = "decision-support-system"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pandas==2.3.3",
    "pyarrow==25.0.1",
]

[project.optional-dependencies]
dev = [
    "ipykernel==7.3.0",
]
